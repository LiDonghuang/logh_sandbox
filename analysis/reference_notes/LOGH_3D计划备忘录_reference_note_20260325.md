# LOGH 3D计划备忘录 - Reference Note (2026-03-25)

Status: Reference Note  
Authority: None / Reference only  
Implementation: Not authorized by this document

## 1. Source

- Source file: `docs/LOGH_3D计划备忘录.md`
- Source type: human-authored long-term planning memo
- Intake purpose: preserve future-facing context without letting it drift into current runtime authorization

## 2. Why This Note Exists

The main thread is currently bounded to:

- `neutral_transit_v1`
- `v4a` subtractive clarification
- `v4b Candidate A` inspect/plan-only preparation
- 2D-only runtime-facing spec solidification

The 3D memo is useful background, but it is not a governance directive and it must not silently redefine current scope.

## 3. Reference-Only Items Worth Keeping

The memo contains several ideas that are useful as future reference:

- strong separation between objective, formation reference, movement, and final legality
- stable slot identity via fixed initial order
- optional debug emission of expected positions
- future viewer / replay notes, including possible 3D presentation tooling
- future extension pressure for richer frame / extent language

These can inform naming and documentation hygiene, but only within current Governance limits.

## 4. Items Explicitly Not Authorized By This Note

This memo does **not** authorize any of the following:

- 3D runtime implementation
- 3D runtime-facing field tables in current `v0.1` specs
- generalized reference-formation framework
- broad movement rewrite
- baseline replacement
- battle-path activation for Candidate A
- slot reassignment / reflow / role system work

## 5. Current Main-Thread Reading Rule

For the current repo state, this memo may only be read as:

- future reference
- terminology pressure
- extension-note material

It may **not** be read as:

- canonical freeze
- implementation approval
- selector approval
- parameter-semantic reinterpretation

## 6. Immediate Use In This Round

In this round, the memo is only allowed to influence:

- wording of 2D-only upstream specs
- explicit non-field lists that keep 3D terms out of current runtime-facing tables
- future-extension notes inside docs, without changing current implementation scope

## 7. Bottom Line

`docs/LOGH_3D计划备忘录.md` is now explicitly registered as a reference-only planning input.

It can support future `v4b` and later 3D discussion, but it does not change the current thread boundary:

- 2D-only specs
- Candidate A structural draft
- inspect/plan-only work
