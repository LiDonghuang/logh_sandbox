# Step 3 3D V4A Shared Default-Review Discussion Note

- Engine Version: `dev_v2.0`
- Modified Layer: `analysis / shared default-review discussion only`
- Affected Parameters: none in committed runtime
- New Variables Introduced: none
- Cross-Dimension Coupling: none
- Mapping Impact: none
- Governance Impact: opens bounded discussion of whether `v4a` is credible as a shared neutral+battle default candidate while retaining `v3a`
- Backward Compatible: yes
- Summary: explains why `v4a` is now review-eligible across both neutral and battle paths, what current evidence supports that read, what still blocks any default switch, and how `v3a` remains retained during the review period

## Purpose

This note responds to governance authorization to discuss `v4a` as a shared default-review candidate.

This note does **not** propose an immediate default switch.

It does **not** remove `v3a`.

It only defines why `v4a` is now serious enough to review structurally rather than only as a local side experiment.

## 1. Why `v4a` Is Now Review-Eligible

`v4a` is now review-eligible because the current meaningful bounded probes on the standard-rectangle issue are already being interpreted through the present `v4a` path.

In practice, recent spacing/restore work is no longer asking:

- does `v4a` exist as a test-only branch?

It is asking:

- is `v4a` already the branch that produces the most meaningful bounded engineering evidence for both neutral-transit and maintained battle-path thinking?

That is enough to justify a review line.

## 2. Current Repo-Side Evidence Supporting This Read

Current repo-side evidence supports `v4a` review eligibility because:

- the standard-rectangle root-cause line has been investigated through the current maintained `v4a` path
- the spacing-decoupling probe remained strong on more than one bounded comparison path
- recent hardening discussion is no longer using `v4a` as a trivial one-off prototype, but as the meaningful active candidate line for movement/restore questions

This still does not prove that `v4a` should become default.

It only proves that default-review discussion is now credible.

## 3. Current Uncertainties That Still Block Any Default Switch

The following still block any credible default switch:

1. the standard-rectangle issue is still not fully closed
2. two-layer spacing remains discussion-only and pre-implementation
3. battle-path evidence is still weaker than neutral-transit fixture evidence
4. a formal replacement standard has not yet been satisfied
5. `v3a` still remains the retained legacy line and cannot be displaced by momentum

So `v4a` is discussion-eligible, not switch-ready.

## 4. Required Proof Standard Before Any Future Default Review Could Advance

Before governance could even consider:

- bounded default-candidate acceptance
- wider baseline review
- or a default switch

the proof standard should include at least:

1. stable evidence across both neutral and battle contexts
2. direct human-readable evidence that improvements are real
3. evidence that improvements are not achieved by hidden hardcoding or silent layer recoupling
4. a clear retention story for `v3a`
5. explicit alignment with baseline-replacement discipline rather than drift-by-use

## 5. How `v3a` Remains Retained During This Review

During this review period, `v3a` remains retained by default because:

- no switch is being authorized here
- no deletion is being authorized here
- no statement is being made that `v4a` has already won the review

So the current correct read is:

- `v3a` remains retained
- `v4a` becomes a serious shared default-review candidate

## Bottom Line

The bounded conclusion is:

- `v4a` is now strong enough to be discussed as a shared neutral+battle default candidate
- but current evidence still falls short of any legitimate default-switch approval
- `v3a` therefore remains retained while the review line stays open
