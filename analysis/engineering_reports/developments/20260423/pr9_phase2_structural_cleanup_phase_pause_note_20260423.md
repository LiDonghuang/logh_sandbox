# PR9 Phase II - Structural Cleanup Phase Pause Note

Date: 2026-04-23  
Scope: continuity status memo only  
Status: paused

## Status

The current structural-cleanup line is paused.

Accepted current reference state:

- post-subtraction dual-posture structural anchor remains the active structural
  reference
- strict subtraction ledger remains the current delete / keep / defer map
- reader-facing stale-surface cleanup is complete
- VIZ now reads `reopen` / `late_reopen_persistence` instead of stale
  `viol` / `relation_violation_severity`

## Boundary

Runtime-side subtraction is not the next default task.

Do not continue structural code work by default on:

- behavior-bearing runtime blocks
- Unit-local maneuver / back-off carriers
- battle-gap behavior-line usage
- locomotion realization paths

Further runtime-side subtraction, behavior optimization, locomotion capability
work, owner wiring migration, or module split requires a new explicit
governance gate.

## Ongoing Discipline

Subtraction remains an engineering discipline, not a standalone optimization
target.

Continue to avoid:

- unnecessary helpers
- silent fallback
- unnecessary fallback
- compatibility residue without an active reason
- helper / fallback surfaces that blur Behavior line vs locomotion capability
  line, fleet authorization vs Unit realization, or maintained path vs
  test-only experimental path

This note does not authorize new code cleanup or behavior work.
