# TS Migration Input

## Objective and Requested Outcomes
Close out this long-running visualization/export/BRF support thread and hand off to a fresh thread with bounded scope.

Current migration objective:
- preserve the active `test_run` visualization/export baseline
- preserve the active BRF narrative resource/render split baseline
- stop further patch chaining in this thread
- let the next thread resume from a clean, bounded target

Recommended next bounded targets for the new thread:
- `export/default parity follow-up` only, if any new divergence is observed
- or `next visualization surface change` only
- or `BRF narrative style pass` only, without mixing with visualization work

## Completed / In-Progress / Pending
### Completed
- `test_run/test_run_v1_0_viz.py`
  - side plots now include a vertical tick cursor that advances with Battlefield tick
  - side-plot A/B legend font reduced and legend placement changed to initialization-time best placement, then frozen
  - export path corrected so overlay geometry is computed after final layout margins are locked
  - export path corrected so Battlefield name/avatar block and other overlay positions better match default mode
  - export path corrected to prefill first-frame overlay state before encoding, reducing the pre-t=1 brightness/content jump
- `test_run/test_run_v1_0.py`
  - export video filename stem aligned with BRF export stem
- `test_run/battle_report_builder.py`
  - BRF narrative generation refactored into semantic-slot layer + rendering layer
- `test_run/brf_narrative_messages.py`
  - message resources externalized and then replaced with the approved resource text block
- user manually ran mp4 export comparison on 2026-03-11 and reported the result as good after the latest export-consistency fixes

### In Progress
- none

### Pending
- no required pending code work for TS itself
- if future export drift is seen again, validate both `full_plot=true` and `full_plot=false` paths explicitly in a new thread

## Critical Constraints
- Follow `AGENTS.md`
- Relevant rules for continuation:
  - `R-01` Push Back Rule
  - `R-02` Minimal Generation Rule
  - `R-03` Frozen Layer Protection
  - `R-04` No Silent Refactor
  - `R-06` Controlled Scope Execution
  - `R-07` Thread Sustainability self-check
  - `R-09` Commit and Push Discipline
  - `R-10` VS Code Path Presentation
- Frozen / sensitive layers must not be edited without explicit approval:
  - `canonical/`
  - governance files
  - mapping definitions
  - engine skeleton
- Workspace is noisy and dirty, including frozen-layer files; future scoped work must avoid staging or editing unrelated files.
- Do not commit or push unless explicitly requested.

## Files Changed and Why
### Active files in this thread
- `test_run/test_run_v1_0_viz.py`
  - visualization surface refinement
  - export/default parity fixes
  - side-plot cursor/legend behavior
  - overlay initialization ordering
- `test_run/test_run_v1_0.py`
  - export video naming aligned with BRF naming stem
- `test_run/battle_report_builder.py`
  - structural BRF narrative refactor into semantics/render separation
- `test_run/brf_narrative_messages.py`
  - approved BRF narrative message catalog integrated

### Related baseline/config files
- `test_run/test_run_v1_0.settings.json`
  - active run settings baseline used during validation
- `archetypes/archetypes_v1_5.json`
  - active archetype baseline referenced by test runs
- `docs/animation_contract_v1.md`
  - active animation/plot contract file; treat as the visualization contract reference for future alignment checks

## Validation Evidence
### Reproducible validation commands
- `python -m py_compile test_run/test_run_v1_0_viz.py`
- `python -m py_compile test_run/test_run_v1_0.py test_run/test_run_v1_0_viz.py`
- `python -m py_compile test_run/battle_report_builder.py test_run/brf_narrative_messages.py`
- `python test_run/test_run_v1_0.py`

### Key outputs and anchors
- current branch: `dev_v1.1`
- latest matched BRF/video export pair found in workspace:
  - `analysis/exports/battle_reports/20260311/test_run_v1_0_20260311_003427_Battle_Report_Framework_v1.0.md`
  - `analysis/exports/videos/test_run_v1_0_20260311_003427_video.mp4`
- user-reported validation:
  - manual mp4 export comparison run on 2026-03-11 looked good after the latest parity fixes

## Workspace State
- branch: `dev_v1.1`
- workspace is dirty and includes many unrelated modified/untracked files
- `git status --short` at handoff included relevant active files such as:
  - `test_run/battle_report_builder.py`
  - `test_run/test_run_v1_0.py`
  - `test_run/test_run_v1_0.settings.json`
  - `test_run/test_run_v1_0_viz.py`
- it also included many unrelated modified/untracked files in frozen or report-heavy areas, for example:
  - `AGENTS.md`
  - `canonical/Strategic_Archetype_Layer_v1.5.md`
  - `runtime/engine_skeleton.py`
  - large trees under `analysis/engineering_reports/developments/...`
  - large trees under `analysis/exports/...`
- future scoped work must stage only the intended files.

## Open Risks and Unknowns
1. `test_run/test_run_v1_0_viz.py` now carries many responsibilities (Battlefield, side plots, debug block, export path, overlay geometry); future mixed-scope changes will be regression-prone.
2. Export/default parity is improved and manually validated, but dual-path rendering remains inherently fragile if more layout logic is added without bounded testing.
3. The thread has accumulated repeated micro-patches over multiple topics; continuing in this same thread would increase omission risk.
4. The workspace dirty state is large enough that any future commit requires very deliberate staging discipline.

## Recommended Next 1-3 Actions
1. Start a new thread for any further visualization/export work and choose one bounded objective only.
2. If the next task is export-related, restrict initial scope to `test_run/test_run_v1_0_viz.py` and validate both default preview and mp4 export.
3. Before any future commit, stage only approved-scope files and ignore the unrelated dirty tree.

## Baseline Anchor
Use this as the baseline anchor for the next thread:
- active run settings: `test_run/test_run_v1_0.settings.json`
- active visualization implementation: `test_run/test_run_v1_0_viz.py`
- active BRF narrative builder: `test_run/battle_report_builder.py`
- active BRF narrative resources: `test_run/brf_narrative_messages.py`
- latest matched export pair:
  - `analysis/exports/battle_reports/20260311/test_run_v1_0_20260311_003427_Battle_Report_Framework_v1.0.md`
  - `analysis/exports/videos/test_run_v1_0_20260311_003427_video.mp4`
- active visualization contract reference:
  - `docs/animation_contract_v1.md`
