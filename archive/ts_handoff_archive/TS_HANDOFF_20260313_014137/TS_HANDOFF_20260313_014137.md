# TS Migration Input

## Objective and Requested Outcomes
Close out the current visualization/export thread and migrate to a fresh thread before any further non-trivial changes.

Current migration objective:
- preserve the current `test_run` visualization baseline
- preserve the recent export-path fixes and rollbacks exactly as they stand now
- preserve the BRF narrative split baseline without mixing it into the next visualization thread unless explicitly requested
- stop further patch chaining in this thread

Recommended bounded next-thread targets:
- `export/default parity verification only`
- or `visual parameter consolidation in test_run_v1_0_viz.py only`
- or `targeted battlefield layout pass only`

## Completed / In-Progress / Pending
### Completed
- `test_run/test_run_v1_0_viz.py`
  - side plots include a vertical tick cursor that moves with Battlefield tick
  - side-plot A/B legends were reduced in size and changed to initialization-time best placement, then frozen
  - export path pre-fills first-frame overlay state before encoding to reduce the pre-t=1 brightness/content jump
  - post-battle camera now returns toward default full view during the extra tail ticks instead of staying zoomed in
  - post-battle zoom-return speed was tuned twice:
    - first too fast
    - then too slow due to linear zoom-ratio scaling
    - current state uses a milder scale: `max(1.0, zoom_ratio / 2.0)` so `2x -> 1x` is roughly unchanged and `4x -> 1x` is about 2x slower
  - Battlefield axis was shifted slightly right so the left-side `Y` label is less clipped, without moving plots/DEBUG
  - export-size parsing was consolidated via `_resolve_export_video_spec(...)`
  - small cleanup landed in the same file: several confirmed-unused variables removed
- `test_run/test_run_v1_0.py`
  - video export naming remains aligned to BRF export naming stem from earlier work in this thread family
- `test_run/battle_report_builder.py`
  - BRF narrative generation remains on the semantic-slot + rendering split baseline from earlier in the thread family
- `test_run/brf_narrative_messages.py`
  - approved resource catalog remains integrated

### In Progress
- none

### Pending
- export/default parity is still sensitive and should be re-verified in a fresh thread after any additional layout change
- if continuing visualization work, the next likely task is parameter consolidation inside `test_run/test_run_v1_0_viz.py`

## Critical Constraints
- Follow `AGENTS.md`
- Relevant rules for continuation:
  - `R-01` Push Back Rule
  - `R-02` Minimal Generation Rule
  - `R-03` Frozen Layer Protection
  - `R-04` No Silent Refactor
  - `R-06` Controlled Scope Execution
  - `R-07` Thread Sustainability self-check
  - `R-09` Commit and Push Discipline
  - `R-10` VS Code Path Presentation
- Frozen / sensitive layers must not be edited without explicit approval:
  - `canonical/`
  - governance files
  - mapping definitions
  - engine skeleton
- Keep future scope bounded to one theme at a time; this file has already accumulated many micro-patches.
- Do not commit or push unless explicitly requested.

## Files Changed and Why
### Active files in this thread family
- `test_run/test_run_v1_0_viz.py`
  - visualization surface behavior
  - export/default parity work
  - battlefield overlay geometry
  - side-plot legend/cursor behavior
  - post-battle auto-zoom return behavior
  - export size-path cleanup
- `test_run/test_run_v1_0.py`
  - export naming alignment with BRF stem
- `test_run/battle_report_builder.py`
  - BRF narrative structure split into semantics/rendering
- `test_run/brf_narrative_messages.py`
  - approved BRF narrative resource catalog

### Related baseline/config files
- `test_run/test_run_v1_0.settings.json`
  - active run and export baseline used during recent validation
- `test_run/test_run_v1_0.viz.settings.json`
  - active visualization tuning baseline
- `archetypes/archetypes_v1_5.json`
  - active archetype baseline referenced by test runs
- `docs/animation_contract_v1.md`
  - active visualization contract reference

## Validation Evidence
### Reproducible validation commands
- `python -m py_compile test_run/test_run_v1_0_viz.py`
- `python -m py_compile test_run/test_run_v1_0.py test_run/test_run_v1_0_viz.py`
- `python -m py_compile test_run/battle_report_builder.py test_run/brf_narrative_messages.py`
- `python test_run/test_run_v1_0.py`

### Key outputs and anchors
- current branch at handoff: `main`
- latest visible BRF outputs under current work:
  - `analysis/exports/battle_reports/20260313/test_run_v1_0_20260313_013951_Battle_Report_Framework_v1.0.md`
  - `analysis/exports/battle_reports/20260313/test_run_v1_0_20260313_013809_Battle_Report_Framework_v1.0.md`
- latest known video artifact in workspace from this line:
  - `analysis/exports/videos/test_run_v1_0_20260313_013005_video.mp4`
- user feedback anchors from this thread tail:
  - non-export path layout looked good
  - export path attempts based on work-area matching were not accepted and were rolled back
  - user requested migration after single-file cleanup + export-path fix

## Workspace State
- branch: `main`
- workspace is dirty and includes unrelated modified/untracked files
- `git status --short` at handoff included relevant active files such as:
  - `test_run/battle_report_builder.py`
  - `test_run/brf_narrative_messages.py`
  - `test_run/test_run_v1_0.py`
  - `test_run/test_run_v1_0.settings.json`
  - `test_run/test_run_v1_0_viz.py`
- it also included unrelated or sensitive files such as:
  - `AGENTS.md`
  - `archetypes/archetypes_v1_5.json`
  - untracked trees under `analysis/engineering_reports/developments/20260311/`
  - untracked docs under `docs/architecture/major_changes/`
- future staged changes must be tightly scoped.

## Open Risks and Unknowns
1. `test_run/test_run_v1_0_viz.py` still carries too many responsibilities: battlefield layout, side plots, debug block, export path, overlay geometry, camera behavior.
2. Export/default parity remains fragile because layout equality depends on both figure size and overlay geometry timing.
3. The current export-size fix ensures the code path uses configured `width_px/height_px`, but direct artifact-level verification was not completed in this environment because the shell environment lacks matplotlib/video-inspection packages.
4. The slight Battlefield right-shift was added as a tactical fix; if a future parameter consolidation happens, that shift should become an explicit layout constant rather than remaining inline logic.

## Recommended Next 1-3 Actions
1. Start a new thread and choose one bounded objective only:
   - `parameter consolidation in test_run_v1_0_viz.py`
   - or `export/default parity verification`
   - or `battlefield layout cleanup`
2. If the next task is export-related, verify both:
   - the printed export log line (`size=...`)
   - and the actual encoded mp4 dimensions / visual match
3. If the next task is cleanup-related, keep scope strictly inside `test_run/test_run_v1_0_viz.py` and avoid mixing BRF/runtime work.

## Baseline Anchor
Use this as the baseline anchor for the next thread:
- active run settings: `test_run/test_run_v1_0.settings.json`
- active visualization settings: `test_run/test_run_v1_0.viz.settings.json`
- active visualization implementation: `test_run/test_run_v1_0_viz.py`
- active BRF narrative builder: `test_run/battle_report_builder.py`
- active BRF narrative resources: `test_run/brf_narrative_messages.py`
- latest visible BRF output:
  - `analysis/exports/battle_reports/20260313/test_run_v1_0_20260313_013951_Battle_Report_Framework_v1.0.md`
- latest known video artifact:
  - `analysis/exports/videos/test_run_v1_0_20260313_013005_video.mp4`
- visualization contract reference:
  - `docs/animation_contract_v1.md`
