# TS Migration Input

Created: 2026-02-26 20:50:09 (local)  
Prepared By: Codex (Engineering thread)  
Human Operator: Zixu

## 1. Code Baseline Anchor

- Branch: `main`
- HEAD commit: `1681c3a14b79113f035c7f344d9ab34a904b6078`
- Workspace state: dirty (uncommitted + untracked files present)
- Dirty file set at handoff:
  - `M AGENTS.md`
  - `M analysis/test_run_v1_0.py`
  - `?? analysis/test_run_v1_0.viz.settings.json`
  - `?? analysis/test_run_v1_0_viz.py`
  - `?? docs/animation_contract_v1.md`

## 2. Change Landing Status

- Submitted commit: No
- Local-only changes: Yes
- Reproducibility note: next thread must start from the same dirty workspace or first create an explicit local commit/tag.

## 3. Validation Commands (Reproducible)

Working directory: `e:\logh_sandbox`  
Python: `3.11.9`

### Command A - Syntax gate

```powershell
$env:MPLBACKEND='Agg'; .\.venv_check\Scripts\python.exe -m py_compile .\analysis\test_run_v1_0.py .\analysis\test_run_v1_0_viz.py
```

Result: PASS

### Command B - Headless runtime gate

```powershell
$env:MPLBACKEND='Agg'; $env:PYTHONPATH='.'; .\.venv_check\Scripts\python.exe .\analysis\test_run_v1_0.py
```

Result: PASS  
Important: without `PYTHONPATH='.'`, `ModuleNotFoundError: runtime_v0_1` may occur.

### Timing sample

```powershell
$env:MPLBACKEND='Agg'; $env:PYTHONPATH='.'; $t=Measure-Command { .\.venv_check\Scripts\python.exe .\analysis\test_run_v1_0.py *> E:\logh_sandbox\archive\ts_handoff_archive\test_run_v1_0_headless_timed.log }; "$([math]::Round($t.TotalSeconds,3))"
```

Observed wall-clock: `7.626s` (single sample)

## 4. Artifacts to Carry

- `analysis/test_run_v1_0.py`
- `analysis/test_run_v1_0_viz.py`
- `analysis/test_run_v1_0.viz.settings.json`
- `analysis/test_run_v1_0.settings.json`
- `docs/animation_contract_v1.md`
- `AGENTS.md`
- `v5.0-alpha1_Contact_Hysteresis_Execution_Spec.md`

Additional handoff artifacts generated in this folder:

- `archive/ts_handoff_archive/test_run_v1_0_headless.log`
- `archive/ts_handoff_archive/test_run_v1_0_headless_timed.log`
- `archive/ts_handoff_archive/test_run_v1_0.settings.baseline.json`
- `archive/ts_handoff_archive/viz_baseline_seedless.png`

## 5. FSR (v5.1) Definition and Delivery Pack

### FSR Full Name

Formation Scale Relaxation (FSR)

### Current Governance Phase

Specification-only (no runtime code modification authorized)

### Required Deliverable

`v5.1-draft-FSR Engineering Execution Spec` (document only, no code)

### Proposed Spec Template (for next thread)

1. Engine Version / Scope / Layer Target  
2. Structural Intent and Non-negotiables  
3. Whitelist / Denylist  
4. Variables & Parameters (defaults, safe ranges, interpretation)  
5. Insertion Point in tick pipeline  
6. Backward Compatibility (`FSR_ENABLED=false` or `fsr_strength=0`)  
7. Validation Gates and report format  
8. Out-of-scope statement  

### DoD (Definition of Done)

- Governance-required fields complete
- Pipeline insertion point explicit
- Whitelist explicit
- Backward-compatibility statement explicit
- No raw code, no runtime edits

### Out-of-Scope (hard)

- No movement objective redesign
- No combat/targeting/participation changes
- No strategy/retreat logic
- No cross-layer refactor

## 6. Frozen Layer Boundaries (Path-Level)

### Denylist for editing

- `canonical/**`
- `runtime/**` (for FSR spec phase)
- `runtime_v0_1.py` (for FSR spec phase)
- Governance baseline records under `docs/phase_chronicles/**` (unless explicitly authorized)

### Controlled files (currently modified, review required before commit)

- `AGENTS.md`
- `analysis/test_run_v1_0.py`
- `analysis/test_run_v1_0_viz.py`
- `analysis/test_run_v1_0.viz.settings.json`
- `docs/animation_contract_v1.md`

## 7. Visualization Baseline Freeze Material

- Fixed-settings baseline file:  
  `archive/ts_handoff_archive/test_run_v1_0.settings.baseline.json`
  - `random_seed=20260227`
  - `background_map_seed=20260227`
- Reference screenshot:  
  `archive/ts_handoff_archive/viz_baseline_seedless.png`
- Performance sample:
  - headless run wall time: `7.626s`
  - nominal simulated ticks: 300
  - nominal captured frames: 300 (`frame_stride=1`)

## 8. Pending Decisions Requiring Human Sign-off

1. FSR spec final document location and naming convention (under `docs/` vs `reports/`).
2. Visualization performance acceptance threshold (time/FPS target) for "optimize without semantic drift".
3. AGENTS governance update policy: whether AGENTS remains open for iterative refinement or enters freeze mode.

## 9. Assumptions (Explicit)

1. Environment remains Python `3.11.9` with venv `.venv_check`.
2. Work continues from current repository state (dirty workspace unless explicitly committed).
3. Phase V.1 FSR remains specification-only until additional governance authorization.
4. Visualization governance follows `docs/animation_contract_v1.md` with Class A/B local and Class C gated.

## 10. Governance Context to Carry Forward

### A. Phase V Visualization Governance Decision (effective)

- Class A/B: local governance (Human + Engineering)
- Class C: governance gate required
- Visualization must not alter runtime/simulation semantics
- Visualization has no canonical authority

### B. Phase V.1 FSR Authorization (current task state)

- Engineering Draft Authorized (specification phase only)
- Submit spec first, no direct runtime modification
- Include whitelist, insertion point, backward compatibility, stability gates

## 11. Next 3 Actions for New Thread

1. Confirm baseline anchor and workspace strategy (continue dirty vs snapshot commit).
2. Produce `v5.1-draft-FSR Engineering Execution Spec` document using the DoD/template above.
3. Run a quick TS check after spec completion before any code-phase proposal.

## 12. Directive to Receiving Engineering Thread (Execution Instruction)

Role:

- You are Engineering execution authority under Dual-Core Protocol v1.1.
- Do not reinterpret governance or canonical mapping.

Primary objective (current phase):

- Deliver `v5.1-draft-FSR Engineering Execution Spec` only.
- Do not modify runtime code in this phase.

Execution sequence (mandatory):

1. Re-anchor baseline:
   - Confirm branch/HEAD/workspace dirty state exactly as recorded.
   - If baseline differs, stop and report drift before any further action.
2. Reproduce validation gates:
   - Run py_compile and headless command exactly as listed in section 3.
   - Record pass/fail and environment variables used.
3. Produce FSR spec document:
   - Use section 5 template and DoD.
   - Include whitelist, insertion point, backward-compatibility statement, and validation plan.
   - Keep scope strictly at specification level.
4. Boundaries check:
   - Confirm denylist paths were not edited.
   - Confirm no runtime or behavior-layer modification.
5. Report:
   - Use Engineering Reporting Standard v1.0 format.
   - Include changed files (if any), assumptions, and residual risks.
6. TS check:
   - Perform one TS self-check before closing the turn.

Hard constraints:

- No code edits under `runtime/**` for this phase.
- No changes to combat/targeting/movement/projection semantics.
- No hidden heuristics, no nonlinear strategy hooks, no randomization.
- Keep changes reversible and auditable.

If blocked or ambiguous:

- Apply push-back rule.
- Ask one concise clarification and pause.
