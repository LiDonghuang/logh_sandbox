# TS Handoff - 2026-04-04 07:47:03

## Objective

Stabilize the current local `v4a` line after repeated misdiagnosis around:

- `battle 1->4` early center-ahead arc
- `neutral 1->4 / 2->4` single-front outlier
- post-contact disorder and earlier hold/first-contact experiments

Requested outcome for the next thread:

1. stop further drift in the current thread
2. re-establish one trustworthy local anchor
3. isolate the active cause with subtraction-first A/B, one group at a time

## Baseline Anchor

There is no recent trustworthy git commit anchor for the current local mechanism state.

Use the following **human-confirmed local anchor** as the practical baseline for the next thread:

- the moment immediately **before** the request:
  - "这一轮的结果人类观察了，是很合理的。"
  - then:
    - `1. Hold摆动的平滑化`
    - `2. Hold阵型内部的稳定化`

The user explicitly identified that the line became bad **after** that request and the resulting smoothing work.

Treat that as the authoritative regression window.

## Workspace State

- Branch: `eng/two-layer-spacing-first-bounded-impl`
- Remote push: not requested in this phase
- Current worktree: dirty, many local-only changes
- Important: current local state has already had several recent probe rollbacks/removals

`git status --short` at handoff time:

```text
 M .vscode/launch.json
 D archive/ts_handoff_archive/TS_HANDOFF_20260402_154545/TS_HANDOFF_20260402_154545.md
 M test_run/settings_accessor.py
 M test_run/test_run_execution.py
 M test_run/test_run_scenario.py
 M test_run/test_run_v1_0.runtime.settings.json
 M test_run/test_run_v1_0.settings.comments.json
 M test_run/test_run_v1_0.settings.reference.md
 M test_run/test_run_v1_0.testonly.settings.json
 M viz3d_panda/app.py
 M viz3d_panda/docs/3D_VIZ_Debug_Block_Reference_v1.0.md
 M viz3d_panda/unit_renderer.py
 ?? analysis/engineering_reports/developments/20260402/...
 ?? analysis/engineering_reports/developments/20260403/...
 ?? analysis/engineering_reports/developments/20260404/...
 ?? docs/archive/ts_handoff_archive/TS_HANDOFF_20260402_154545/...
 ?? docs/engineering/Runtime_Dynamics_Observation_Gates_v1.0.md
 ?? docs/engineering/Runtime_Settings_Wiring_Gates_v1.0.md
 ?? docs/exports/reports/v4a_current_mechanism_flow_20260403.html
 ?? docs/exports/reports/v4a_current_mechanism_flow_20260403_sources/...
```

## Critical Constraints

- Root `AGENTS.md` and `test_run/AGENTS.md` apply.
- `runtime/engine_skeleton.py` is frozen.
- No commit/push unless explicitly requested.
- Subtraction-first only.
- No more broad theory expansion in-thread.
- User trust is critically low; next thread must avoid speculative root-cause claims.

## What Was Completed

### Active deletions already applied

1. Removed the recent far-phase probe conditions from `test_run/test_run_execution.py`:
   - `battle_relation_gap_current >= 0.25`
   - `engagement_geometry_active_current <= 0.05`

2. Removed dependent far-phase probe branches from `test_run/test_run_execution.py`:
   - `battle_far_axis_chase_disabled`
   - `far_expected_position_bridge_active`
   - `battle_far_motion_axis_locked`

3. Retired hold smoothing public knobs and active-path use:
   - `runtime.movement.v4a.battle_hold_relaxation`
   - `runtime.movement.v4a.battle_approach_drive_relaxation`

4. In the active `test_run` path:
   - `battle_relation_gap`
   - `close_drive`
   - `brake_drive`
   - `hold_weight`
   - `approach_drive`
   now collapse back to raw values instead of extra `raw -> current` smoothing.

### Important confirmations

1. The deleted hold-smoothing keys are actually gone from active `test_run` settings/summary surface.
2. The old `v4a` cohesion-like path (`centroid_probe_scale` / earlier `restore_strength` suspicion) is **not** using the prior user-set value in active `v4a`.
   - In `test_run/test_run_execution.py`, when `movement_model == "v4a"`:
     - `movement_surface["centroid_probe_scale"] = 1.0`

## What Was Not Resolved

1. `battle 1->4` early center-ahead arc still persists.
2. `neutral 1->4 / 2->4` single-front outlier still persists.
3. Multiple attempted local battle probes failed to improve the arc visually.
4. Root cause is **not yet isolated** with sufficient confidence.

## Most Important User-Corrected Read

The next thread should trust these user-level corrections:

1. The bad window starts **after** the request to do:
   - hold oscillation smoothing
   - hold internal stabilization

2. The early arc is:
   - pre-contact
   - center ahead of wings
   - visible around `tick < 100`

3. `contact` is **not** a one-time event and must not be treated as such.

4. `hold` itself is now broadly on the right conceptual path; the current main issue is not "invent a new hold doctrine".

## Changed Files and Why

### Core active file

- `test_run/test_run_execution.py`
  - holds almost all recent local battle experiments and deletions
  - this is the main isolation target

### Settings surface files

- `test_run/settings_accessor.py`
- `test_run/test_run_scenario.py`
- `test_run/test_run_v1_0.testonly.settings.json`
- `test_run/test_run_v1_0.settings.comments.json`
- `test_run/test_run_v1_0.settings.reference.md`
  - updated when hold smoothing public knobs were retired

### Viewer/docs touched but not part of the immediate root-cause isolation

- `.vscode/launch.json`
- `viz3d_panda/app.py`
- `viz3d_panda/unit_renderer.py`
- `viz3d_panda/docs/3D_VIZ_Debug_Block_Reference_v1.0.md`

### Analysis records created during this thread

- `analysis/engineering_reports/developments/20260404/step3_3d_pr6_v4a_near_contact_parameter_surface_update_record_20260404.md`
- `analysis/engineering_reports/developments/20260404/step3_3d_pr6_hold_smoothing_retirement_record_20260404.md`
- plus many other 20260402-20260404 local records

Do not treat those records as proof of correctness; they are chronology, not acceptance.

## Validation Evidence

Commands already run successfully during the latest cleanup:

```powershell
python -m py_compile e:\logh_sandbox\test_run\settings_accessor.py e:\logh_sandbox\test_run\test_run_scenario.py e:\logh_sandbox\test_run\test_run_execution.py
rg -n "battle_hold_relaxation|battle_approach_drive_relaxation|v4a_battle_hold_relaxation_effective|v4a_battle_approach_drive_relaxation_effective" e:\logh_sandbox\test_run
```

Observed result:

- no active `test_run` hits for the retired hold-smoothing keys

Additional confirmation command used:

```powershell
@'
from pathlib import Path
import sys
base = Path(r'e:\logh_sandbox')
sys.path.insert(0, str(base / 'test_run'))
from test_run_scenario import prepare_active_scenario
obj = prepare_active_scenario(base / 'test_run')
summary = obj['summary']
for k in [
    'v4a_battle_hold_relaxation_effective',
    'v4a_battle_approach_drive_relaxation_effective',
    'v4a_battle_relation_lead_ticks_effective',
    'v4a_battle_target_front_strip_gap_bias_effective',
    'centroid_probe_scale_effective'
]:
    print(f'{k}={summary.get(k, "<missing>")}')
'@ | python -
```

Observed summary:

```text
v4a_battle_hold_relaxation_effective=<missing>
v4a_battle_approach_drive_relaxation_effective=<missing>
v4a_battle_relation_lead_ticks_effective=18.0
v4a_battle_target_front_strip_gap_bias_effective=-4.0
centroid_probe_scale_effective=<missing>
```

## Open Risks / Unknowns

1. The current local worktree contains too many overlapping battle experiments to trust intuitive reasoning alone.
2. It is still unknown whether the early arc and the neutral outlier share a cause.
3. It is still unknown whether the true bad actor is:
   - one surviving active battle-only preprocessing path
   - or accumulated local drift inside `test_run/test_run_execution.py`
4. The current thread is no longer trustworthy for continued broad diagnosis.

## Recommended Next 1-3 Actions

1. **Do not continue broad in-thread debugging.**
   - Start a fresh thread from this handoff.

2. In the next thread, isolate only the current active `test_run_execution.py` battle path with subtraction-first A/B:
   - no new parameters
   - no new mechanism theory
   - one mechanism group at a time

3. First isolation target in the new thread:
   - identify the surviving active battle-only preprocessing that differs from neutral during early pre-contact formation realization
   - do not use old commit guesses as anchors
   - use the user-confirmed regression window instead

## Recommended User Action Right Now

Stop modifying mechanisms in this thread.

The safest next move is:

- preserve current local state
- open a fresh thread using this handoff
- resume with one subtraction-only isolation pass on `test_run/test_run_execution.py`

