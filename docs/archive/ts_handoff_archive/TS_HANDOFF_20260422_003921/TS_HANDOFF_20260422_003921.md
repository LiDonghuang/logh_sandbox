# TS Handoff - 20260422_003921

## Objective

Prepare a clean restart point for the next `viz3d_panda/` thread after the prior VIZ thread became unresponsive.

The Human's current direction is explicit:

- stop spending time on incremental CPU-side micro-optimization of `sway`, `token`, and similar local visual tuning
- treat the larger problem as a CPU-side redraw / scene-update burden
- defer the real optimization push until a future GPU-side / batch-oriented pass
- when that future pass happens, consider the visual family together:
  - token / cluster presentation
  - fire links
  - explosion FX

## Baseline Anchor

- Repo root: `E:\logh_sandbox`
- Current branch: `eng/dev-v2.1-formation-only`
- Current HEAD: `a4b5ce2b7dbaacd337925f2667fae96366860ed4`

## Requested Outcomes - Status

### Completed Before This Handoff

- `viz3d_panda` has a working live viewer and a separate offline export launcher.
- Camera-take recording and export path exist and remain bounded to `viz3d_panda/`.
- Skybox selection has already been decoupled from `runtime.settings` / replay seed semantics:
  - live viewer picks a skybox from VIZ-side true randomness
  - export uses the `skybox_dir_path` saved in the take JSON
- `cluster sway` exists as a viewer toggle and profile-driven effect.

### Current Human Decision

- Do **not** continue chasing small CPU-side optimizations in the current phase.
- Treat future optimization as a likely GPU-side or batch-drawing line of work.
- That future optimization should consider `fire links` and `explosion FX` together with existing visual layers rather than patching them one by one.

### Pending / Good Next Thread Candidates

1. Re-open with a narrow VIZ thread whose first job is **not implementation**, but owner clarification for a future GPU-side visual path.
2. If implementation is requested later, do it as a bounded design/owner slice first, not as another round of scattered CPU-side tuning.
3. Keep any immediate live-viewer fixes extremely local and correctness-oriented only.

## Critical Constraints

- Follow root `AGENTS.md` strictly.
- Stay inside `viz3d_panda/` unless Human explicitly expands scope.
- Do not silently re-couple VIZ behavior to `runtime.settings`.
- Do not silently move replay/runtime ownership into VIZ or vice versa.
- No silent fallback.
- For cleanup or performance turns, use subtraction-first logic.
- Before any new mechanism work, identify:
  - active owner
  - exact hot path
  - whether the issue is CPU scene-graph churn, transparent overdraw, or true rendering backend cost

## Files Changed / Relevant For The Next Thread

- `.vscode/launch.json`
  - contains current VS Code launchers for:
    - `dev_v2.0 Viewer (Panda3D)`
    - `dev_v2.0 Video Export (Panda3D)`
- `viz3d_panda/app.py`
  - live viewer bootstrap
  - camera-take record/save path
  - export pipeline entry (`export_camera_take_video`)
  - current VIZ-side skybox selection carrier
  - current sway toggle binding / status text
- `viz3d_panda/export_video.py`
  - offline export launcher entry
- `viz3d_panda/replay_source.py`
  - bounded viewer replay reconstruction entry
- `viz3d_panda/scene_builder.py`
  - skybox selection and assembly
- `viz3d_panda/unit_renderer.py`
  - token / cluster rendering
  - sway implementation
  - fire-link rendering
- `viz3d_panda/docs/3D_VIZ_Debug_Block_Reference_v1.0.md`
  - currently dirty; review only if the next thread needs the latest VIZ-side operator notes

## Files The Next Thread Should Read First

Read in this order:

1. `viz3d_panda/unit_renderer.py`
2. `viz3d_panda/app.py`
3. `viz3d_panda/scene_builder.py`
4. `viz3d_panda/export_video.py`
5. `.vscode/launch.json`

Optional, only if needed:

6. `viz3d_panda/docs/3D_VIZ_Debug_Block_Reference_v1.0.md`

## Active Mechanism Notes

### Skybox

Active owner:

- `viz3d_panda/scene_builder.py`
- `viz3d_panda/app.py`

Current rule:

- live viewer uses `resolve_random_skybox_dir_path()`
- export does **not** regenerate selection from a seed
- export reuses `replay_request["skybox_dir_path"]` from the saved camera take

This is the current active decoupling from `runtime.settings`.

### Camera-take / Export

Active owner:

- `viz3d_panda/app.py`
- `viz3d_panda/export_video.py`

Current rule:

- viewer can record camera take JSON
- export reads that JSON and renders offscreen through Panda3D
- output launcher is already separated from the main viewer launcher

### Cluster Sway

Active owner:

- `viz3d_panda/unit_renderer.py`
- toggled from `viz3d_panda/app.py`

Important current reality:

- renderer-level default is disabled
- but `app.py` currently turns it on during app bootstrap

That means the earlier intent of "default off" and the current active startup behavior are not aligned.

This mismatch is worth keeping in mind, but the Human's current priority is **not** to spend more time tuning it.

### Performance Framing

Current Human interpretation:

- the dominant problem is large CPU-side redraw / node-update burden
- local tuning of `sway`, `token`, and similar effects has shown weak returns
- future optimization should likely be planned at a wider visual-system level

Recommended future owner framing:

- think in terms of a GPU-side / batch-drawing visual track
- evaluate token/cluster/fire-link/explosion together as one visual-performance family
- avoid reopening isolated CPU micro-optimization unless a very small correctness issue is explicitly requested

## Validation Evidence

This handoff turn did not change mechanism code.

Current syntax baseline check run in this turn:

```powershell
@'
from pathlib import Path
for path in [
    Path('viz3d_panda/app.py'),
    Path('viz3d_panda/export_video.py'),
    Path('viz3d_panda/replay_source.py'),
    Path('viz3d_panda/scene_builder.py'),
    Path('viz3d_panda/unit_renderer.py'),
]:
    compile(path.read_text(encoding='utf-8'), str(path), 'exec')
    print(f'{path}: ok')
'@ | .\.venv_dev_v2_0\Scripts\python.exe -
```

Observed result:

- all five files compiled successfully

## Workspace State

Current workspace is dirty well beyond the VIZ scope.

Tracked dirty files relevant to VIZ restart:

- `.vscode/launch.json`
- `viz3d_panda/app.py`
- `viz3d_panda/docs/3D_VIZ_Debug_Block_Reference_v1.0.md`
- `viz3d_panda/replay_source.py`
- `viz3d_panda/scene_builder.py`
- `viz3d_panda/unit_renderer.py`

Tracked dirty files also exist outside VIZ scope, including:

- `README.md`
- `runtime/engine_skeleton.py`
- several `test_run/*` files

Untracked analysis/governance artifacts also exist under:

- `analysis/engineering_reports/developments/20260419/`
- `analysis/engineering_reports/developments/20260420/`
- `analysis/engineering_reports/developments/20260421/`
- `analysis/reference_notes/...`
- `docs/governance/PR9_Phase2_Unit_Solving_Layer_Governance_Direction_20260419.md`

The next VIZ thread should assume the worktree is shared and noisy, and stage carefully.

## Open Risks / Unknowns

- The exact active delta from the unresponsive VIZ thread was not fully narrated back in-thread; the code state is more trustworthy than any partial memory.
- `cluster sway` remains a likely contributor to perceived runtime cost, but the Human does not currently want more local tuning.
- `fire link` is still visually sensitive and should not be "optimized" by making it feel static.
- A future GPU-side track needs a careful owner decision before code is written.
- There is a known intent/behavior mismatch around sway startup default.

## Recommended Next 1-3 Actions

1. Start the next VIZ thread with a narrow goal: produce a design/owner note for a future GPU-side or batch-drawing visual path, instead of immediately coding another CPU-side tweak.
2. Treat `viz3d_panda/unit_renderer.py` as the primary hot-path audit file; identify which visual families should eventually share the same acceleration strategy.
3. Only if the Human asks for a tiny correctness fix before that larger design pass: handle local issues such as explicit default toggles or export-path correctness, and avoid broader performance experimentation.

## TS Assessment

Migration is recommended and this handoff is ready to use as the next-thread input bundle.

Reason:

- prior VIZ thread became unresponsive
- current VIZ work has accumulated enough mechanism history that a fresh thread will be safer
- the Human now wants a clearer strategic reset: pause local CPU tuning and reserve serious optimization for a future GPU-oriented pass
