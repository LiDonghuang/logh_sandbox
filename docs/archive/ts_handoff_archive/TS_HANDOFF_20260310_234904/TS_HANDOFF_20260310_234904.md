# TS Migration Input

## Objective and Requested Outcomes
Stabilize and continue the `test_run` / BRF / observation line without further context drift.
Current immediate objective at handoff:
- pause after BRF narrative family audit
- stop further BRF micro-patching in this thread
- resume from a new thread with a bounded target

Recommended next bounded target:
- either accept BRF narrative as "good enough" and return to mechanism / observation mainline
- or perform one final, explicit, bounded BRF pass limited to `test_run/battle_report_builder.py` and `test_run/brf_narrative_messages.py`

## Completed / In-Progress / Pending
### Completed
- `test_run/` and `archetypes/` moved out of `analysis/` root and committed earlier on `dev_v1.1`
- runtime collapse source baseline in test harness switched to `v3_test @ 1.1`
- 1-9 parameter scale normalization landed in active runtime path
- ODW prototype path implemented in movement `v3a`
- plot surface heavily refined:
  - active slots now:
    - `Alive`
    - `FireEff`
    - `LossRate`
    - `Coh_v2 / Coh_v3`
    - `CollapseSig`
    - `SplitSep`
    - `FrontCurv`
    - `Wedge`
    - `C_W_PShare`
- `plot_smoothing_ticks` added as display-only setting
- DEBUG block refined and documented
- BRF builder stabilized once:
  - legacy builder removed
  - duplicate helpers merged
  - section render split into smaller functions
  - summary build / profile classification / narrative assembly partially separated
- BRF narrative messages reorganized:
  - priorities added
  - compact variants added
  - wording upgraded toward battle-chronicle tone
- `Wedge` observer metric corrected away from old PCA-axis implementation to enemy-axis-based implementation
- BRF narrative `event_endgame` converted from misleading timed event wording to phase-summary wording:
  - now `其后，会战转入终盘。`
- Current run was actually re-run from current settings to confirm narrative issue source:
  - latest rerun BRF: `analysis/exports/battle_reports/20260310/test_run_v1_0_20260310_234700_Battle_Report_Framework_v1.0.md`

### In Progress
- BRF line is in acceptable-but-not-final state
- `Wedge` is currently retained, but only after observer bugfix; still should be treated as secondary compared with `FrontCurv`

### Pending
- decide whether BRF line stops here or gets one final bounded pass
- return focus to mechanism / observation mainline
- if continuing BRF, likely next target is not wording but slot-family / semantics hardening only

## Critical Constraints
- Follow `AGENTS.md`
- Relevant rules:
  - `R-01` Push Back Rule
  - `R-03` Frozen Layer Protection
  - `R-06` Controlled Scope Execution
  - `R-07` Thread Sustainability self-check
  - `R-09` Commit and Push Discipline
- Frozen / sensitive layers:
  - canonical docs
  - governance files
  - engine skeleton only by explicit exception
- Current thread should not continue broadening scope
- Do not commit or push unless explicitly requested
- There are many unrelated dirty/untracked files in workspace; keep them out of any future scoped commit

## Files Changed and Why
### Active BRF / test-run layer
- `test_run/battle_report_builder.py`
  - single-file stabilization
  - summary / front-profile / narrative assembly cleanup
  - endgame narrative changed to phase-summary wording
- `test_run/brf_narrative_messages.py`
  - message priorities
  - compact variants
  - battle-report wording upgrades
- `test_run/test_run_v1_0.py`
  - import fixes (`Any`, `Sequence`)
  - plot/debug telemetry and indicator plumbing over many turns
  - corrected `Wedge` observer calculation to enemy-axis-based version
- `test_run/test_run_v1_0_viz.py`
  - plot surface and labels refined
  - display-only smoothing and display ranges
  - debug block refinement
- `test_run/test_run_v1_0.settings.json`
  - visualization settings and active runtime/test settings evolved
- `docs/animation_contract_v1.md`
  - synchronized active plot surface / hidden indicators / debug payload

### Related docs / reports
- `analysis/engineering_reports/developments/20260308/phase_runtime_collapse_signal_source_evaluation/...`
  - many ODW / collapse / plot governance notes and DOE outputs
- `analysis/exports/battle_reports/20260310/...`
  - many recent BRFs used for BRF/narrative validation

## Validation Evidence
### Reproducible validation commands
- `python -m py_compile test_run/test_run_v1_0.py`
- `python -m py_compile test_run/test_run_v1_0_viz.py`
- `python -m py_compile test_run/battle_report_builder.py`
- `python -m py_compile test_run/brf_narrative_messages.py`
- `python test_run/test_run_v1_0.py`

### Key outputs
- Current branch: `dev_v1.1`
- Latest actual rerun under current settings exported:
  - `analysis/exports/battle_reports/20260310/test_run_v1_0_20260310_234700_Battle_Report_Framework_v1.0.md`
- This rerun confirmed the root issue was narrative phase semantics, not raw event data:
  - raw timeline still shows `Endgame Onset = 320`
  - but narrative now correctly reads phase-summary after swing:
    - `...双方于标准时05:57 (t=357) 前后发生一次战术拉扯，兵力优势一度易手。 其后，会战转入终盘。 ...`

## Workspace State
- Branch: `dev_v1.1`
- Workspace is still dirty and noisy
- `git status --short` at handoff includes active modified files such as:
  - `test_run/battle_report_builder.py`
  - `test_run/test_run_v1_0.py`
  - `test_run/test_run_v1_0.settings.json`
  - `test_run/test_run_v1_0_viz.py`
  - `docs/animation_contract_v1.md`
  - plus many unrelated modified/untracked files
- Important: many unrelated files exist in `analysis/engineering_reports/developments/...`, `analysis/exports/...`, `visual/...`, and docs/canonical areas. Do not accidentally stage them.

## Open Risks and Unknowns
1. BRF line has been repaired many times in one thread; even after stabilization, there may still be hidden coupling in slot-family logic.
2. `Wedge` is now on the corrected observer path, but should still be treated cautiously; it may remain secondary to `FrontCurv`.
3. `C_W_PShare` remains somewhat noisy and likely auxiliary rather than core.
4. The workspace has very large unrelated dirty/untracked state, which increases commit risk.
5. The current thread has accumulated repeated context resets and patch chains; further edits here are likely to degrade reliability.

## Recommended Next 1-3 Actions
1. Start a new thread from this handoff and choose one bounded objective:
   - `BRF stop-point confirmation`
   - or `return to mechanism / observation mainline`
2. If BRF continues, limit scope explicitly to:
   - `test_run/battle_report_builder.py`
   - and, if wording is required, `test_run/brf_narrative_messages.py`
   - no runtime / observer metric changes unless a real bug is proven
3. Before any future commit, stage only approved-scope files and ignore the large unrelated dirty set

## Baseline Anchor
Use this as the baseline anchor for the next thread:
- current settings run: `test_run/test_run_v1_0.settings.json`
- latest confirmed BRF under current settings: `analysis/exports/battle_reports/20260310/test_run_v1_0_20260310_234700_Battle_Report_Framework_v1.0.md`
- current active BRF files:
  - `test_run/battle_report_builder.py`
  - `test_run/brf_narrative_messages.py`
- current active plot contract:
  - `docs/animation_contract_v1.md`
