# TS Handoff Memo

## Objective

Close the Panda3D engineering sub-thread with a short handoff for the Engineering main thread.

Requested outcomes:

- summarize the scoped 3D viewer/export work completed in this thread
- record the current workspace state and validation evidence
- preserve governance constraints for the next thread

## Status

Completed:

- added viewer-local camera take recording in `viz3d_panda/`
- added strict camera-take offline video export with explicit `ffmpeg` handling
- fixed offline export stale-node overlap around replay tick `420`
- separated offline export from the main Panda3D viewer entry
- added a dedicated offline render launcher
- replaced PNG-sequence export with direct Panda3D framebuffer piping into `ffmpeg`
- added preset-based offline export launch options: `low`, `standard`, `high`
- exported a clearly labeled partial video from the interrupted `6589`-frame sequence
- added explicit trimming for invalid terminal take samples that request interpolation past the final replay frame

In progress:

- none

Pending:

- no requested follow-up at thread close

## Baseline Anchor

- Scope stayed inside `viz3d_panda/`, launcher files, and local viewer/export behavior
- No `runtime/` changes
- No `test_run/` semantics changes
- No frozen-layer / canonical / governance document edits

## Files Changed And Why

- `viz3d_panda/app.py`
  - added camera take recording, REC status UI, strict export helpers, export reconstruction fix, narrowed the main entry back to live-viewer-only, then switched offline export from PNG screenshots to direct raw-frame piping into `ffmpeg`
- `viz3d_panda/camera_controller.py`
  - added minimal camera snapshot/apply helpers for take recording and offline reconstruction
- `viz3d_panda/export_video.py`
  - new dedicated offline export entry using current default paths, offscreen rendering, and preset-based export selection
- `launch_dev_v2_0_offline_render.bat`
  - reduced to a single-purpose launcher for the dedicated export entry
- `.vscode/launch.json`
  - viewer config includes the default camera-take JSON output path, and video export now exposes a preset picker in VS Code

Unrelated dirty files not touched by this thread:

- `test_run/test_run_v1_0.runtime.settings.json`
- `test_run/test_run_v1_0.testonly.settings.json`
- `analysis/engineering_reports/developments/20260402/` (untracked)

## Key Outcomes

- Camera take default JSON path:
  - `analysis/exports/videos/dev_v2_0_camera_take.json`
- Video export default directory:
  - `analysis/exports/videos`
- Offline export progress prints every `10%`
- Offline export now runs as a dedicated entry:
  - `python -m viz3d_panda.export_video`
- Offline export now streams raw `rgb24` frames directly to `ffmpeg`; it no longer writes intermediate PNG frame directories
- Offline export uses Panda3D offscreen rendering with presets:
  - `low = 5fps / 800x600`
  - `standard = 10fps / 1024x768`
  - `high = 30fps / 1920x1080`
- VS Code video export launch defaults to `standard`
- Current take file has `12` invalid terminal samples that attempt to interpolate past the final replay frame; export trims them explicitly and reports the trim
- Main viewer help no longer exposes export CLI flags

## Validation Evidence

Reproducible checks run in this thread:

```powershell
.\.venv_dev_v2_0\Scripts\python.exe -m viz3d_panda.app --help
.\.venv_dev_v2_0\Scripts\python.exe -m viz3d_panda.export_video --help
cmd /c launch_dev_v2_0_offline_render.bat
.\.venv_dev_v2_0\Scripts\python.exe -m viz3d_panda.export_video --preset standard --output-mp4 analysis\exports\videos\dev_v2_0_camera_take_pipe_standard.mp4
```

Additional targeted probe used during the tick-420 bugfix:

- inspected take samples around replay tick `420`
- confirmed replay removal at `419 -> 420` for unit `B:B1`
- verified repaired offline reconstruction no longer retained the removed node
- inspected the tail of `dev_v2_0_camera_take.json`
- confirmed the current take ends with `12` samples on the final replay frame that still request interpolation
- verified export now trims these terminal samples explicitly before rendering

Latest successful export artifact from this thread:

- `analysis/exports/videos/dev_v2_0_camera_take_pipe_standard.mp4`
- size observed at thread close: `33,628,165` bytes
- last write observed at thread close: `2026-04-02 18:49:42`

Additional export artifact from interrupted-frame recovery:

- `analysis/exports/videos/dev_v2_0_camera_take_partial_6589frames.mp4`
- size observed at thread close: `79,805,547` bytes
- last write observed at thread close: `2026-04-02 17:58:34`

Offscreen confirmation:

- runtime probe reported Panda3D output type `GraphicsBuffer / wglGraphicsBuffer`, not `GraphicsWindow`

## Workspace State

Not committed.
Not pushed.
Main scoped files remain locally modified / added.
Other unrelated dirty files are present in the workspace and were not normalized by this thread, including `repo_context.md`, `system_map.md`, `test_run/test_run_execution.py`, `test_run/test_run_v1_0.runtime.settings.json`, `test_run/test_run_v1_0.testonly.settings.json`, and `viz3d_panda/replay_source.py`.

## Open Risks / Unknowns

- offscreen export was verified via Panda3D buffer type, not by external desktop capture inspection
- `high` preset remains materially heavier than `standard`; the new pipe path removes PNG I/O overhead but does not remove all framebuffer readback cost
- the current take-tail trim is deliberately narrow and only handles terminal samples that interpolate past the final replay frame; broader take sanitation was not added

## Carried Governance Constraints

- obey root `AGENTS.md`
- preserve frozen-layer protection
- no silent fallback
- keep scope inside approved files unless the human re-expands it
- if future work becomes structural again, follow inspect -> plan -> small edit -> review

## Recommended Next Actions

1. If Engineering main thread wants to keep this work, review scoped diffs in `viz3d_panda/` and launcher files only.
2. If a commit is requested later, keep unrelated dirty `test_run/` settings files out of scope.
3. If export UX is revisited, decide whether the preset-only VS Code flow is enough or whether explicit per-run CLI overrides should be surfaced more broadly.
