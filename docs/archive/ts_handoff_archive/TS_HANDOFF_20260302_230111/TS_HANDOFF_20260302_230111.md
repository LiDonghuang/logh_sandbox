# TS Handoff - 20260302_230111

## TS Verdict
- Verdict: 建议迁移新线程
- Reason Level: High

## Trigger Signals
1. 线程历史已跨越多个阶段（V2.1, V3, V4a, V4b, V4c, V4d）且目标类型混合（runtime、diagnostic、reporting、文档规范）。
2. 工作区处于高噪声 dirty 状态（modified=20, added=20, deleted=3），跨主题变更并存，连续推进时遗漏风险高。
3. 近几轮产物密集（多个分钟级报告目录），上下文重建与证据追踪成本显著上升。
4. 用户与 Governance 指令频繁切换“行为变更 / shadow-only / 文档规范”，需要更清晰的单目标线程边界。

## Objective and Requested Outcomes
- 当前主目标：完成 Phase V.4-d（Cohesion v3.1 connectivity calibration）shadow 观测提交，保持“无行为切换”。
- 紧随目标：等待 Governance 对 v3.1 结构判断，再决定是否进入后续开关阶段。

## Baseline Anchor
- Engine baseline: v5.0-alpha5（active decision path 仍为 cohesion_v2）。
- Branch: `main`
- HEAD: `b2debe9ba39a6117955cb1973ce0dbcc4728e913`
- Workspace state: dirty (`modified=20`, `added=20`, `deleted=3`)

## Completed Status
1. Phase V.4-c shadow 产物已生成（cohesion_v3）并给出 determinism digest。
2. Phase V.4-d shadow 产物已生成（cohesion_v3.1 relative connectivity）并给出 determinism digest。
3. 运行设置已切换为“两参数主控”语义：
   - `contact_hysteresis_h<=0 => CH disable`
   - `fsr_strength<=0 => FSR disable`
   - 保留 legacy `ch_enabled/fsr_enabled` 兼容读取（若存在则作为 AND 门控）。

## In-Progress Status
1. v4c 目录存在重复快照（0216 与 0219）；正式可用目录为 0219，0216 为重复残留。
2. 老报告模板与新规则对齐已做部分调整，但历史目录未统一清理。

## Pending Status
1. Governance 审阅 V4-d 结果后的下一步裁决（是否继续结构 refinement 或授权切换）。
2. 若需要提交 commit/push，需先由人类明确授权并限定提交范围（R-09）。
3. 处理重复/废弃报告目录的治理策略（保留或归档）。

## Critical Constraints
1. 遵守 AGENTS.md：Push Back / Minimal Generation / Frozen Layer / No Silent Refactor / Controlled Scope。
2. 当前轮为 shadow-only：不得修改 collapse/PD/movement/combat 行为路径。
3. Canonical 层文件改动需 Governance 明确授权；不做擅自重解释。

## Files Changed and Why (This Thread Key Delta)
1. `runtime/engine_skeleton.py`
   - 新增 `cohesion_v3` shadow 观测函数与 debug 缓存（默认 OFF），不接行为路径。
2. `analysis/test_run_v1_0.py`
   - 运行时门控语义改为强度参数主控（<=0 disable），并兼容旧布尔键读取。
3. `analysis/test_run_v1_0.settings.json`
   - runtime 区移除 `ch_enabled/fsr_enabled`，保留并说明强度参数语义。
4. `analysis/phase_v_default.settings.json`
   - 同步移除 `ch_enabled/fsr_enabled`，与主控语义一致。
5. `analysis/engineering_reports/20260302_0219_phase_v4c_cohesion_v3_shadow/*`
   - V4-c shadow 产物与提交草稿。
6. `analysis/engineering_reports/20260302_0249_phase_v4d_cohesion_v3p1_shadow/*`
   - V4-d shadow 产物（4 CSV + proposal）。

## Validation Evidence
1. Syntax check:
   - `python -m py_compile runtime/engine_skeleton.py`
   - `python -m py_compile analysis/test_run_v1_0.py`
2. V4-c determinism (FR8_MB8_PD5 double-run digest):
   - `ffabdb0a73eb2df982f5d7a90308c2ea154e941e9668d9102ed9cb4a0bb098aa`
3. V4-d determinism (FR8_MB8_PD5 double-run digest):
   - `3b1976cb6e9a08dcd2753bda58699b6def2081b67f6a1d244e946642ad747883`
4. V4-d关键观测：
   - `C_conn_rel(t0)` 在 3x3x3 所有 case-side = 1.0
   - `C_conn_abs(t0)` 仍低（mean 0.185）

## Reproducible Validation Commands
1. `python -m py_compile runtime/engine_skeleton.py analysis/test_run_v1_0.py`
2. `python analysis/test_run_v1_0.py`  (using current `analysis/test_run_v1_0.settings.json`)
3. 快速核查 V4-d 产物:
   - `Get-ChildItem analysis/engineering_reports/20260302_0249_phase_v4d_cohesion_v3p1_shadow`
   - `Get-Content analysis/engineering_reports/20260302_0249_phase_v4d_cohesion_v3p1_shadow/cohesion_v3p1_shadow_tick01_summary.csv -First 20`

## Open Risks and Unknowns
1. 工作区多主题 dirty 状态可能导致提交污染与证据链混淆。
2. 旧报告目录重复（0216/0219）与历史模板差异可能引发引用歧义。
3. v3.1 当前在基线下 `C_scale(t0)=1`，后续是否覆盖更多拓扑极端场景需 Governance 决策。

## Recommended Next 1-3 Actions
1. 新开线程，仅承接 V4-d Governance 审阅与后续单目标动作（避免继续叠加历史噪声）。
2. 在新线程先冻结“本次可提交产物清单”，再执行 commit/push（若人类授权）。
3. 单独处理重复目录与历史产物归档策略，避免与功能开发耦合。

## Notes
- 本 handoff 未生成 headless log 文件，符合 R-07 附件限制。

## Addendum - Requested By New Thread (2026-03-02)

### 1) Governance 对 V4-d 的最新裁决原文
- 已确认的最新 Governance 原文：`Phase V.4-d - Cohesion v3.1 Connectivity Calibration (Shadow Only)` 指令文本（本轮输入即该文本）。
- 当前未发现后续“通过/拒绝/开关授权”的新裁决原文（即：尚未看到从 shadow 进入行为开关阶段的正式批准文本）。
- 因此当前有效状态仍是：`Shadow Only / No Behavior Switch`.

### 2) 本轮“允许提交”文件白名单（R-09 限定）
- 建议白名单（仅覆盖本轮已执行范围）：
  1. `runtime/engine_skeleton.py`  (cohesion_v3 shadow observer support)
  2. `analysis/test_run_v1_0.py`  (runtime two-knob enable semantics)
  3. `analysis/test_run_v1_0.settings.json`  (remove ch_enabled/fsr_enabled; docs sync)
  4. `analysis/phase_v_default.settings.json`  (same semantics sync)
  5. `analysis/engineering_reports/20260302_0219_phase_v4c_cohesion_v3_shadow/*`
  6. `analysis/engineering_reports/20260302_0249_phase_v4d_cohesion_v3p1_shadow/*`
- 非白名单文件（本轮不建议提交）：其余 dirty 文件，尤其 canonical/doc/report 历史遗留改动。

### 3) `20260302_0216` 与 `20260302_0219` 重复目录处理决定
- 工程侧建议决定：
  - `20260302_0219_phase_v4c_cohesion_v3_shadow` 作为 authoritative 版本。
  - `20260302_0216_phase_v4c_cohesion_v3_shadow` 标记为重复残留（obsolete duplicate）。
- 执行时机建议：
  - 在下次“获授权的整理提交”中执行一次性处理（归档或删除二选一）。
  - 若 Governance 要求保留审计痕迹，则优先“归档并加 DEPRECATED 标注”；若无要求则删除。

### 4) Frozen 层精确路径清单（当前线程执行约束）
- 绝对只读（默认）：
  1. `canonical/**`
  2. `canonical/archive/**`
- 治理协议/治理规范（视同 frozen）：
  3. `canonical/Sandbox_Governance_Canonical_v1.0.md`
  4. `canonical/Sandbox_CrossThread_Protocol_v1.1.md`
  5. `canonical/Phase_Transition_Governance_Playbook_v1_0.md`
  6. `canonical/Engine_v2.0_Skeleton_Canonical.md`
- Mapping 定义（视同 frozen）：
  7. `canonical/Mapping_Snapshot_*.md`
  8. `canonical/Ten_Parameters_Canonical_Index_v*.md`
  9. `canonical/Ten_Parameters_Three_Class_Mapping_Essay_v*.md`
  10. `canonical/Strategic_Archetype_Layer_v1.5.md`

### Assumptions (Explicit)
1. “最新裁决”以仓库内可检索文本与本线程接收指令为准；未检索到更晚裁决即视为未下达。
2. 白名单用于“本轮最小提交”而非全仓清理提交。
3. 重复目录处理需先满足 R-09（人类明确授权提交动作）。
