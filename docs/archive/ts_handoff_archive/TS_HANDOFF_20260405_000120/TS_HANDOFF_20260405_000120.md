# TS HANDOFF - 2026-04-05 00:01:20

Status: active Engineering thread start package  
Purpose: restart work in a fresh Engineering thread after the previous AGENTS-focused thread was terminated  
Authority: migration aid only; not merge approval, not default-switch approval

## 1. Objective and Requested Outcome

The next Engineering thread should **not** continue the abandoned AGENTS-discussion line.

Its mission is:

- continue the active cleanup program with a concrete engineering focus
- respect the governance-locked order
- keep scope narrow and subtraction-first

The highest-priority practical line now is:

1. preserve the recovered `dev_v2.1` anchor
2. keep targeting in bounded stabilization mode
3. prepare the next real old-mechanism cleanup step:
   - `restore_strength` ownership re-rooting / decoupling

This means the new thread should begin by establishing one trustworthy local anchor and one truthful ownership map for the decoupling work.

It should **not** restart broad theory expansion, AGENTS debate, or mixed cleanup.

## 2. Current Governance Read

Governance has already locked the cleanup order as:

1. stale claims / ownership truth surface
2. targeting stabilization
3. `restore_strength` ownership re-rooting / decoupling
4. subtraction-first retirement of old families
5. continued structure tightening
6. only then later compute optimization

Immediate interpretation for the next thread:

- stale-claim cleanup already has a docs opening in PR `#7`
- targeting has an active bounded candidate and should not be rapidly widened
- the next serious old-mechanism cleanup target is **not** generic `v3a` deletion
- it is specifically:
  - `restore_strength` decoupling from the old `v3_test` carrier

## 3. Completed / In Progress / Pending

### Completed

- `dev_v2.1` recovered anchor exists on remote
- `v4a.restore_strength` was reactivated and Human confirmed it recovered the earlier battle / neutral behavior
- near-contact smoothing group was restored and pushed to `dev_v2.1`
- a first targeting candidate was added in `runtime/engine_skeleton.py`
- PR `#7` was opened and accepted as a cleanup opening carrier
- stale-claim cleanup and current ownership map docs were started

### In Progress

- local, uncommitted targeting stabilization changes exist
- local, uncommitted AGENTS cleanup changes exist
- TS handoff path migration to `docs/archive/ts_handoff_archive/` is now active

### Pending

- commit or discard the current local targeting stabilization edits in a controlled way
- perform `restore_strength` ownership re-rooting / decoupling
- only after that, begin subtraction-first retirement of old mechanism families

## 4. Current Branch and Workspace State

Current branch:

- `eng/dev-v2.1-cleanup-opening`

Recent commits:

- `9c67602` `docs: open v2.1 cleanup line and ownership map`
- `a1e5f1d` `v2.1: add expected-damage targeting candidate and governance update`
- `787bf0d` `v2.1: restore v4a near-contact smoothing group`
- `cfd8ecc` `v2.1: reactivate v4a restore strength bridge`

Current local dirty files:

### Group A - AGENTS / protocol-only cleanup

- `AGENTS.md`
- `test_run/AGENTS.md`
- `analysis/engineering_reports/developments/20260404/step3_3d_pr6_agents_cleanup_rule_update_record_20260404.md`
- `analysis/engineering_reports/developments/20260404/step3_3d_pr6_ts_handoff_path_rule_update_record_20260404.md`

### Group B - targeting / runtime local work

- `runtime/engine_skeleton.py`
- `test_run/test_run_scenario.py`
- `test_run/test_run_v1_0.runtime.settings.json`
- `README.md`
- `analysis/engineering_reports/developments/20260404/step3_3d_pr6_targeting_logic_expected_damage_candidate_record_20260404.md`

### Group C - old TS path migration residue

- deleted old-root handoff path:
  - `archive/ts_handoff_archive/TS_HANDOFF_20260404_074703/TS_HANDOFF_20260404_074703.md`
- untracked new docs-side handoff directories:
  - `docs/archive/ts_handoff_archive/TS_HANDOFF_20260404_074703/`
  - `docs/archive/ts_handoff_archive/TS_HANDOFF_20260404_233833/`

Important read:

- these groups must not be silently mixed
- the next Engineering thread must decide its scope before touching any of them

## 5. Truthful Current Ownership Read

### Active targeting owner

- `runtime/engine_skeleton.py::resolve_combat()`

Current local targeting candidate reads:

- `angle_quality = max(0, 1 + alpha * cos(theta))`
- `range_quality = 1.0` inside `attack_range * fire_optimal_range_ratio`
- linear decay to `0.0` at `attack_range`
- `expected_damage_ratio = angle_quality * range_quality`
- target score currently uses:
  - `normalized_hp / expected_damage_ratio`

Current local runtime settings candidate:

- `fire_quality_alpha = 0.33`
- `fire_optimal_range_ratio = 0.5`
- clamp widened locally to `[0.0, 1.0]`

### Current `restore_strength` truth

`restore_strength` is currently active, but not yet cleanly rooted:

- harness surface:
  - `runtime.movement.v4a.restore_strength`
- bridge:
  - `test_run/test_run_execution.py`
  - writes `movement_surface["centroid_probe_scale"]`
- runtime carrier:
  - old `v3_test` centroid-probe path

This is the core transitional dependency that the next structural cleanup step must address.

## 6. Critical Constraints

- obey root `AGENTS.md` and `test_run/AGENTS.md`
- do not treat chronology as proof of causality
- do not mix AGENTS cleanup with mechanism cleanup in one commit
- do not broaden targeting doctrine
- do not begin old-family mass retirement before `restore_strength` decoupling
- do not silently move targeting params into `testonly.settings`
- keep runtime hot-path changes explicit and bounded

## 7. Validation Evidence Already Available

Previously completed and relevant checks:

- `python -m py_compile runtime/engine_skeleton.py test_run/settings_accessor.py test_run/test_run_scenario.py test_run/test_run_execution.py`
- local summary checks showed:
  - `fire_quality_alpha_effective = 0.33`
  - `fire_optimal_range_ratio_effective = 0.5`
- Human read already accepted:
  - restored `restore_strength` recovered the earlier battle / neutral feel
  - the first targeting opening looked broadly reasonable in 3D

These observations are useful anchors, but they do not replace a clean scope decision for the next thread.

## 8. Open Risks and Unknowns

1. The workspace contains multiple unrelated dirty groups.
   - Risk: accidental mixed commit or mixed diagnosis.

2. `restore_strength` is still alive through an old runtime carrier.
   - Risk: broad old-family cleanup attempted too early will break the recovered anchor.

3. Targeting local edits are present but not yet cleanly packaged.
   - Risk: cleanup thread drifts into mechanism finishing work.

4. AGENTS cleanup is still locally dirty.
   - Risk: Human-facing policy cleanup gets confused with mechanism cleanup again.

## 9. Recommended Next 1-3 Actions

### Recommended Action 1

Start the new Engineering thread by choosing one narrow scope:

- either finalize and package the current local targeting stabilization edits
- or start `restore_strength` decoupling planning

Do **not** attempt both in the same first turn.

### Recommended Action 2

If the chosen scope is old-mechanism cleanup, first produce one short truth map:

- what currently owns `restore_strength`
- what old carrier it still depends on
- what candidate native `v4a` owner will replace that carrier
- what exact files are in scope

### Recommended Action 3

Once the scope is fixed, isolate the relevant dirty group and keep all other dirty files out of the change set.

## 10. Reproducible Startup Commands

Use these first in the new thread:

```powershell
git branch --show-current
git status --short
git diff -- runtime/engine_skeleton.py test_run/test_run_scenario.py test_run/test_run_v1_0.runtime.settings.json README.md analysis/engineering_reports/developments/20260404/step3_3d_pr6_targeting_logic_expected_damage_candidate_record_20260404.md
git diff -- AGENTS.md test_run/AGENTS.md analysis/engineering_reports/developments/20260404/step3_3d_pr6_agents_cleanup_rule_update_record_20260404.md analysis/engineering_reports/developments/20260404/step3_3d_pr6_ts_handoff_path_rule_update_record_20260404.md
```

## 11. Bottom Line

The next Engineering thread should start from this read:

- the recovered anchor is real
- targeting is open but should stay bounded
- the next meaningful old-mechanism cleanup step is `restore_strength` ownership re-rooting
- the workspace is dirty in multiple independent groups, so scope discipline is now the first practical requirement
